# Synergy Review — CRITICAL + HIGH Fixes Applied

Source reports:
- `reports/synergy-review-phases-1-3.md`
- `reports/synergy-review-phases-4-5.md`

Mode: precise edits only. No `pnpm` / `tsc` / `git` runs.

| Fix # | Severity | File(s) modified | Change summary | Verified in-file? |
|-------|----------|-------------------|----------------|--------------------|
| 1 | CRITICAL | `tasks/phase-2/task-2-1.md`, `tasks/phase-1/task-1-5.md` | Task 2.1 now re-exports `FrameContext` + `Landmark` from `./types` (single source of truth). Task 1.5 types.ts pins `Landmark = NormalizedLandmark`. Task 2.1 removes the redeclared types, pins `create(gl: WebGL2RenderingContext): EffectInstance` signature (no `deps` param), and adds a `ModulationSourceDef.extract` import of `Landmark`. Task 1.5 gets a "single source of truth" gotcha. | Yes |
| 2 | CRITICAL | `tasks/phase-3/task-3-3.md`, `tasks/phase-3/task-3-4.md`, `tasks/phase-3/README.md` | Renamed `computeColumnEdges` / `computeRowEdges` references to the canonical `generateColumnWidths` / `generateRowWidths`. Task 3.4's manifest Task 4 now uses `generateColumnWidths(seed, count, variance)` and `generateRowWidths(seed, count, variance)` and prepends a leading 0 before multiplying by videoW/H. | Yes |
| 3 | CRITICAL | `tasks/phase-1/task-1-6.md`, `tasks/phase-2/task-2-R.md`, `tasks/phase-3/task-3-1.md`, `tasks/phase-3/task-3-5.md`, `tasks/phase-3/task-3-R.md`, `tasks/phase-2/task-2-3.md`, `tasks/phase-2/task-2-4.md` | Canonicalized canvas selectors. Task 1.6 Stage now emits a nested `<div data-testid="render-canvas">` alias inside the stage wrapper (alongside `data-testid="stage"`). Replaced every `canvas[data-role="webgl"]` with `canvas[data-testid="webgl-canvas"]` in Tasks 2.R / 3.1 / 3.5 / 3.R. Replaced every `data-testid="stage-2d-canvas"` with `data-testid="overlay-canvas"` in Tasks 2.3 and 2.4. | Yes |
| 4 | CRITICAL | `tasks/phase-2/task-2-1.md`, `tasks/phase-3/task-3-4.md` | Pinned `EffectManifest.create(gl: WebGL2RenderingContext): EffectInstance` in Task 2.1 (no `deps` parameter). Task 3.4 now MODIFIES `src/engine/renderer.ts` to add module-scoped `getRenderer()` / `getVideoTexture()` getters; `manifest.create(gl)` sources renderer + texture via those. `MosaicRenderDeps` removed from Data Models. Task 3.4 old Task 4/5 (modifying `src/engine/manifest.ts` and `renderLoop.ts`) deleted. | Yes |
| 5 | CRITICAL | `tasks/phase-3/task-3-4.md`, `tasks/phase-4/task-4-5.md` | Task 3.4 gains a "compositing for captureStream" gotcha + per-frame draw order starting with `ctx.drawImage(webglCanvas, 0, 0)` on the 2D overlay (with mirror transform applied via `ctx.save(); ctx.scale(-1,1); ctx.translate(-w,0); ... ctx.restore()` when `params.input.mirrorMode === true`). Task 4.5 All Needed Context notes 2D overlay IS the composited surface post-3.4; acceptance criterion "Downloaded .webm plays back WITH the mosaic visible" added. | Yes |
| 6 | CRITICAL | `tasks/phase-1/task-1-3.md` | `ErrorStates.tsx` card div now carries `data-testid={\`error-state-card-${state}\`}`; `PrePromptCard.tsx` carries `data-testid="error-state-card-PROMPT"`. Success criteria updated. (Task 5.4 clarification: GRANTED happy path asserts `not.toBeVisible()` on prefix selector.) | Yes |
| 7 | CRITICAL | `tasks/phase-1/task-1-6.md` | `render-canvas` test-id alias added as a nested full-bleed `<div>` inside the stage wrapper (unambiguous, zero-cost). Success criteria mention both IDs resolving to the same element-region. | Yes |
| 8 | HIGH | `tasks/phase-2/task-2-5.md`, `tasks/phase-3/task-3-1.md`, `tasks/phase-4/task-4-3.md`, `tasks/phase-4/task-4-R.md`, `tasks/phase-5/task-5-1.md`, `tasks/phase-2/README.md` | Every `src/app/main.tsx` reference replaced with `src/main.tsx`. `src/app/registerSW.ts` → `src/registerSW.ts`. Phase 2 README notes flat `src/` layout (no `src/app/` subfolder). | Yes |
| 9 | HIGH | `tasks/phase-1/task-1-5.md` | `startRenderLoop` accepts `overlayCtx2d?: CanvasRenderingContext2D \| null`; `FrameContext.ctx2d` field added (`CanvasRenderingContext2D \| null`), populated by every tick. Task 1.6's App.tsx integration adds the wiring note. Downstream effects never early-return on missing ctx2d. | Yes |
| 10 | HIGH | `tasks/phase-1/task-1-6.md`, `skills/hand-tracker-fx-architecture/SKILL.md` | Stage.tsx gains `onVideoReady?: (el: HTMLVideoElement) => void` prop invoked in srcObject effect after `play()` resolves/rejects. App.tsx owns the render-loop lifetime via `useState<HTMLVideoElement \| null>`. Skill gains a "Stage.tsx Evolution" section enumerating Tasks 1.6 / 3.1 / 3.4 / 3.5 modifications. | Yes |
| 11 | HIGH | `skills/hand-tracker-fx-architecture/SKILL.md`, `tasks/phase-2/task-2-5.md` | Skill gains a "Dev Hook Contract" table enumerating every `window.__handTracker` member with signature + introducing task (isReady, isUsingGpu, getFPS, getLandmarkCount, listEffects, getParam, setParam, getLandmarkBlobCount, lastGridLayout, getVideoTextureHandle, setFakeLandmarks, computeActiveRegions, getLastRegionCount, forceContextLoss, forceContextRestore). Task 2.5 Task 7 adds `setParam(dotPath, value)` to devHooks modification (required by Task 2.R). | Yes |
| 12 | HIGH | `tasks/phase-1/task-1-6.md` | Removed the `preserveDrawingBuffer: true ready for Phase 3` bullet from Task 1.6 Final Validation Checklist. Anticipatory note remains in "Why". Verification is now the responsibility of Task 3.1. | Yes |
| 13 | HIGH | `tasks/phase-1/task-1-2.md` | Task 1.2 Implementation Task 3 adds the `?forceState=<STATE>` URL-param short-circuit (gated by `DEV \|\| MODE === 'test'`, validated against `CAMERA_STATES`) as a first-class step. Prerequisite for Phase 2/3/4/5 E2Es. | Yes |
| 14 | HIGH | `tasks/phase-5/task-5-4.md` | Task 5.4 Task 1 no longer modifies useCamera.ts (it's a prerequisite from 1.2). PROMPT forcing pinned to `?forceState=PROMPT` URL param; "dropped-fake-UI-project" alternative explicitly excluded. `chromium-no-ui-fake` project removed from Task 2 and Integration Points. GRANTED describe clarified to use `not.toBeVisible()` on `[data-testid^="error-state-card-"]`. | Yes |
| 15 | HIGH | `tasks/phase-2/task-2-1.md`, `tasks/phase-2/task-2-5.md` | Task 2.1 `ParamDef` `button` variant JSDoc clarifies `allParams` is read-only — mutation is a no-op — use `paramStore.set()` to write. Task 2.5 gotcha rewritten to match (no longer claims the signature "cannot take snapshot as an argument"). | Yes |
| 16 | HIGH | `tasks/phase-3/task-3-3.md` | Task 3.3 front-matter adds `Blocked by: Task 3.2, Task 2.3` and `Dependencies: 3.2, 2.3`. No-Prior-Knowledge bullet updated to reflect dependency on 3.2 for `MAX_REGIONS`. | Yes |

## Notes

- Fix #7 (`render-canvas` alias) is implemented as a nested zero-cost overlay `<div>` inside the stage wrapper because HTML/React cannot carry two distinct `data-testid` values on a single element. The alias element has `position: absolute; inset: 0; pointer-events: none` so it shares the stage's bounding box and is Playwright-visible.
- Fix #4 removes the old Task 3.4 step "MODIFY src/engine/manifest.ts (type signature)" and the step "MODIFY src/engine/renderLoop.ts (pass deps to create)". The replacement is a new step "MODIFY src/engine/renderer.ts (add module-scoped getters)" sequenced BEFORE the manifest.ts modification.
- Fix #5's mirror transform is applied inside Task 3.4's per-frame `ctx.drawImage(webglCanvas, 0, 0)` step — NOT via CSS (which doesn't affect `captureStream` output).
- MEDIUM + LOW issues are logged in `reports/synergy-review-backlog.md`. They were not applied.
